/**
 * 外边距
 */
export default class VMargin {
    constructor(top, bottom, left, right) {
        this.top = top || 0;
        this.bottom = bottom || 0;
        this.left = left || 0;
        this.right = right || 0;
    }
}